

To install docker  ----------->>> sudo apt install docker.io -yield

To know the version ---------->>> docker --version

To check the docker info =------>>>> docker info

To see all the images in local ----->>> docker images

To find the images in docker hub ----->> docker search imageName

To download image form docker hub  ---->> docker pull imageName

To build the image ------------------->>> docker buiild -it .To

To run the image --------------------->>> docker run -it imageName 

To start the container ----------------->>>docker start conatainerName

To remove conatiner -------------------->>> docker rm conatainerName

To chekc the conatiner we created those are running ------>>> docker ps -a